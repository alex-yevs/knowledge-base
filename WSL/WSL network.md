
## 📡 Определение IP-адреса

```PowerShell
wsl hostname -I
```

Возвращает IP-адрес дистрибутива Linux, установленный через WSL 2 (адрес виртуальной машины WSL 2)


```PowerShell
ip route show | grep -i default | awk '{ print $3}'
```

Возвращает IP-адрес компьютера Windows, как видно из WSL 2 (виртуальная машина WSL 2)


Более подробное описание см. в разделе [Доступ к сетевым приложениям с помощью WSL: определение IP-адреса](https://learn.microsoft.com/ru-ru/windows/wsl/networking#identify-ip-address).